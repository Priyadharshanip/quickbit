
# Server port ? each service runs on a different port
server.port=8081

# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:5432/quickbite_auth
spring.datasource.username=postgres
spring.datasource.password=priya123

# JPA ? auto creates tables from your Java classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# App name ? Eureka will use this later
spring.application.name=auth-service

jwt.secret=quickbite-super-secret-key-that-is-long-enough-for-hs256-algorithm
jwt.expiration=86400000